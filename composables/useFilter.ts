import type { FilterData } from "~/assets/filter"
// import getSort from "~/utils/getSort"

// export function useFilter<T>(data: T[], key: keyof FilterData) {
//   const search = (data: T[], key: keyof T) =>
//     data.filter((d: T) => {
//       // @ts-ignore
//       return d[value].toLowerCase().includes(store.search.toLowerCase())
//     })

//   // stock High and Low
//   const sort = (value: string) => getSort(data, "key", value)

//   return { sort, search }
// }
