// will show market profile
// find by session user
// and find market from session user
// return market profile

export default defineEventHandler(async (event) => {
  const data = {
    toJson(): { test: { test: string } } {
      return {
        test: { test: "not implement" },
      }
    },
  }
  return data
})
